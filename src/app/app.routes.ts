import { Routes } from '@angular/router';
import { SquaresGameComponent } from './squares-game/squares-game.component';

export const routes: Routes = [
  { path: '', component: SquaresGameComponent }
];
